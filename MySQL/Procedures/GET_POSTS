DELIMITER //

CREATE PROCEDURE GET_POSTS ()
BEGIN
  SELECT 
    P.POST_ID,
    P.AUTHOR_ID,
    U.USERNAME,
    P.CONTENT,
    P.CREATED_AT
  FROM POSTS P
  JOIN USERS U ON P.AUTHOR_ID = U.USER_ID
  ORDER BY P.CREATED_AT DESC;
END //

DELIMITER ;